# =============================================================================
# ML-Server-Manager Environment Configuration Template
# =============================================================================
# Usage:
# - For development: Copy to .env.dev (or use the provided .env.dev)
# - For production: Copy to .env and update all CHANGE_ME values
# =============================================================================

# =============================================================================
# Database Configuration
# =============================================================================
POSTGRES_USER=mlsmanager
POSTGRES_PASSWORD=your_db_password
POSTGRES_DB=mlsmanager
# For local development (outside Docker)
DATABASE_URL=postgresql+asyncpg://mlsmanager:your_db_password@localhost:5432/mlsmanager

# =============================================================================
# RabbitMQ Configuration
# =============================================================================
RABBITMQ_DEFAULT_USER=mlsmanager
RABBITMQ_DEFAULT_PASS=your_rabbitmq_password
# For local development (outside Docker)
RABBITMQ_URL=amqp://mlsmanager:your_rabbitmq_password@localhost:5672//
CELERY_RESULT_BACKEND=rpc://

# =============================================================================
# Application Configuration
# =============================================================================
APP_NAME=ML-Server-Manager
APP_VERSION=0.1.0
DEBUG=true                    # Set to false in production
ENVIRONMENT=development       # development | production
HOST=0.0.0.0
PORT=8000

# =============================================================================
# Security Configuration
# =============================================================================
# IMPORTANT: Generate a strong secret key for production!
# Example: openssl rand -hex 32
SECRET_KEY=your-secret-key-change-me-in-production
ACCESS_TOKEN_EXPIRE_MINUTES=1440
ALGORITHM=HS256

# =============================================================================
# CORS Configuration
# =============================================================================
# JSON array of allowed origins
CORS_ORIGINS=["http://localhost:5173","http://localhost:3000"]

# =============================================================================
# Node Configuration
# =============================================================================
NODE_TYPE=master              # master | worker
NODE_ID=master-001

# =============================================================================
# Default Admin User (created on first startup if no users exist)
# =============================================================================
DEFAULT_ADMIN_USERNAME=admin
DEFAULT_ADMIN_EMAIL=admin@example.com
DEFAULT_ADMIN_PASSWORD=change_this_password

# =============================================================================
# Frontend Configuration
# =============================================================================
VITE_API_BASE_URL=http://localhost:8000

# =============================================================================
# Worker Agent Configuration
# =============================================================================
# For local development (outside Docker)
AGENT_MASTER_URL=http://localhost:8000
AGENT_NODE_NAME=worker-001
AGENT_HEARTBEAT_INTERVAL=30
AGENT_JOB_POLL_INTERVAL=10
AGENT_STORAGE_PATH=/data
AGENT_DATASETS_PATH=/data/datasets
AGENT_DEV_MODE=false          # Set to true for development

# =============================================================================
# Build Proxy Configuration (optional, for China network acceleration)
# =============================================================================
# HTTP_PROXY=http://127.0.0.1:7897
# HTTPS_PROXY=http://127.0.0.1:7897
# NO_PROXY=localhost,127.0.0.1
