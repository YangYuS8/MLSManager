# ML-Server-Manager Development Environment - Worker Node
# 环境变量配置模板
# 复制此文件为 .env.worker 并根据需要修改

# =============================================================================
# Master 连接
# =============================================================================
# Master 节点 API 地址
# 同一台机器: http://localhost:8000
# 远程 Master: http://<master-ip>:8000
AGENT_MASTER_URL=http://localhost:8000

# =============================================================================
# 节点标识
# =============================================================================
AGENT_NODE_NAME=dev-worker

# =============================================================================
# Worker Agent API
# =============================================================================
# Worker HTTP API 端口 (供 Master 调用)
AGENT_API_PORT=8081

# =============================================================================
# 存储路径
# =============================================================================
AGENT_STORAGE_PATH=/data
AGENT_PROJECTS_PATH=/data/projects
AGENT_DATASETS_PATH=/data/datasets
AGENT_JOBS_WORKSPACE=/data/jobs

# =============================================================================
# Code-Server
# =============================================================================
CODE_SERVER_PORT=8443

# =============================================================================
# 用户/组 ID (用于文件权限)
# =============================================================================
# 运行 `id` 命令获取你的 UID 和 GID
UID=1000
GID=1000
