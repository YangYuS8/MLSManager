# ML-Server-Manager Production Environment - Worker Node
# 环境变量配置
# 复制此文件为 .env.worker 并修改

# =============================================================================
# Master 连接 (必填)
# =============================================================================
AGENT_MASTER_URL=http://master-node-ip:8000

# =============================================================================
# 节点标识
# =============================================================================
AGENT_NODE_NAME=worker-001

# =============================================================================
# Worker Agent API
# =============================================================================
# Worker HTTP API 端口 (供 Master 调用)
AGENT_API_PORT=8081

# =============================================================================
# 存储路径
# =============================================================================
AGENT_STORAGE_PATH=/data
AGENT_PROJECTS_PATH=/data/projects
AGENT_DATASETS_PATH=/data/datasets
AGENT_JOBS_WORKSPACE=/data/jobs

# =============================================================================
# Code-Server (必填)
# =============================================================================
CODE_SERVER_PASSWORD=CHANGE_THIS_SECURE_PASSWORD
CODE_SERVER_PORT=8443

# =============================================================================
# 用户/组 ID
# =============================================================================
UID=1000
GID=1000
